# AgentSessions —— 跨 AI 编码平台的统一会话管理工具 · 产品规划文档（V3）

> V3 变更：① 正式定名 **AgentSessions**；② 技术栈定为 **Tauri + Node 解析核心**；③ 首批支持 Agent 改为 **Trae / OpenCode / WorkBuddy 系列**；④ 新增 **账号维度** 与 **内容分层 + 子主题渐进披露式搜索**；⑤ 功能**不做取舍，全部保留并深入优化**。

---

## 一、项目概述

**一句话定位**：自动发现本机所有 coding agent 的会话记录，按项目/工作目录归类，支持按**账号维度**与**内容分层（标题→主题→正文→思考）**渐进披露式查找，让你随时找回「那个活是在哪里干的、用的哪个账号、当时怎么聊的」。

**确定名**：AgentSessions

**核心价值主张**
1. **跨工具统一**：Trae、OpenCode、WorkBuddy 系列（后续扩展 Claude/Cursor/Codex/Qwen/Gemini/Copilot/Windsurf）的会话在一个地方看。
2. **按项目聚合 + 账号可辨**：按工作目录一屏看到该项目所有 AI 会话，每个会话标清用的哪个账号。
3. **渐进披露式检索**：像漏斗一样由浅入深——标题 → 主题/子主题 → 正文 → 思考过程，先快筛后聚焦，同时大幅优化搜索性能。

**产品边界**：✅ 自动发现、增量索引、内容分层检索、项目聚合、会话阅读导出、解析逻辑；❌ 不做 AI 对话、代码生成、云端同步、团队协作、token 计费、实时监控告警、移动端。

---

## 二、问题定义与目标用户

### 2.1 痛点
1. 会话散落多工具，无统一入口。
2. 会话与项目/账号关联断裂，无法按项目、按账号聚合。
3. 搜索粒度太粗——要么全局一把梭（慢、杂），要么只在标题，搜不到思考过程。
4. 格式不统一，无法跨工具阅读/导出。

### 2.2 用户画像（三级）
| 等级 | 画像 | 核心诉求 |
|---|---|---|
| L1 重度 | 技术负责人，多仓库多账号 | 秒级检索、按账号/项目审计取证 |
| L2 中度 | 日常开发者，多工具切换 | 快速找回、按项目查看、按账号区分工作/个人 |
| L3 轻度 | 独立开发者/研究者 | 低门槛、导出整理 |

> 优先满足 L2；schema 为 10 万级会话预留。

### 2.3 使用场景（标注频率 + 涉及功能）
| # | 场景 | 频率 | 优先级 | 涉及功能 |
|---|---|---|---|---|
| S1 | **找回某个活在哪、哪个账号干的** | 高频 | P0 | 分层搜索、项目/账号过滤、结果上下文 |
| S2 | **按项目回顾** | 高频 | P0 | 项目聚合、时间线、统计 |
| S3 | **搜思考过程/方案对比** | 中频 | P0 | 深层次全文检索(thinking) |
| S4 | **跨工具继续** | 中频 | P1 | 会话详情、复制上下文、导出 |
| S5 | **按账号审计**：「这是我工作号还是个人号的对话」 | 中频 | P1 | 账号维度筛选、导出 |
| S6 | **决策复盘** | 低频 | P1 | 时间范围、主题树、收藏 |
| S7 | **新人交接/自述整理** | 低频 | P2 | 项目聚合、批量导出 Markdown |
| S8 | **工具整理（排除/不索引）** | 低频 | P1 | 排除规则、敏感过滤 |

> MVP 闭环场景：S1/S2/S3。全部由「采集→分层索引→(项目+账号聚合+渐进式检索)」链路支撑。

---

## 三、功能特性（全量保留 + 深入优化）

### A. 数据采集（P0 基础）
| 功能 | 说明 | 深入优化点 | 优先级 |
|---|---|---|---|
| Agent 自动检测 | 扫描已安装 agent | 首批 Trae/OpenCode/WorkBuddy；架构预留适配器扩展 | P0 |
| 会话文件发现 | 遍历会话目录 | 跟随 symlink 可配置 | P0 |
| 格式解析适配器 | 每 agent 一个→统一模型 | 采集核心统一 **Node**，parse 逻辑 | P0 |
| 🆕 **账号提取** | 从会话元数据/hook 事件提取 account/user | 归一化账号名（work/personal/自定义映射表） | P0 |
| 增量扫描 | mtime+size 指纹 | 二次无改动 <5s | P0 |
| 工作目录提取 | cwd/workspace→project_id | 规则见 §6.3 | P0 |
| 内容提取 | 消息/回复/思考/工具/文件 | 解析失败降级原文索引 | P0 |
| 🆕 **主题/子主题抽取** | 从标题+首段+消息序列聚类出 subject_tree | 规则见 §6.4 | P1 |
| 大文件保护 | >50MB 跳过/采样 | 阈值可配 | P1 |
| 手动触发扫描 | 全量/增量 | 任意时间可点 | P1 |
| 文件监听 | 目录实时变化 | 平台差异 watch | P2 |
| 重复会话去重 | 同 id/同文件去重 | 唯一约束 | P2 |

### B. 数据模型
- 五层模型：Project / Session / Turn / ToolCall / FileChange（见 §7 DDL）。
- 🆕 新增 **account**（账号）与 **content_layer / subject**（内容分层与主题）维度。

### C. 项目 + 账号分类聚合（P0）
| 功能 | 说明 | 优先级 |
|---|---|---|
| 按工作目录聚合 | 同一 cwd 归一个项目 | P0 |
| 🆕 **按账号聚合** | 项目内可再按账号切分，会话标注账号 | P0 |
| 项目自动/自定义命名 | 目录名→可改 | P0 |
| 项目列表视图 | 会话数、agent 分布、账号分布、最近活动 | P0 |
| 项目详情视图 | 会话时间线（标注账号） | P0 |
| 未识别项目 | 归「未分类」，可手动指定 | P1 |
| 项目合并（软链接/repo） | 多 cwd 合并 | P2 |
| 多项目对比 | 两个项目使用对比 | P2 |

### D. 搜索 —— 渐进披露式查找（核心特色，P0）
> **设计思路**：不做一把梭的全局搜索，而是把内容按「披露深度」分层，用户从浅到深逐层钻取；浅层用轻量字段秒回，深处才触发全文本检索，兼顾**专注性**与**性能**。

| 披露深度 | 层 | 描述 | 检索代价 | 优先级 |
|---|---|---|---|---|
| L1 | **标题** | 会话标题/首条 | 最轻，即时 | P0 |
| L2 | **主题/子主题** | 抽取的 subject_tree 节点 | 轻，走独立索引 | P0 |
| L3 | **正文** | AI 回复 + 用户消息 | 中，FTS 全文 | P0 |
| L4 | **思考过程** | thinking / 工具调用参数 | 重，仅在钻取到 L3 顶端时触发 | P0 |
| L5 | 🆕 **账号/来源** | 作为独立过滤维度叠加 | 索引过滤 | P0 |

**L1→L5 递进规则**
- 顶层默认只搜「标题+主题」（L1/L2），**命中即展示、零全表扫**。
- 结果不足/需更深时，逐级展开到正文(L3)、思考(L4)。
- 账号(L5)、项目、时间、agent 作为**正交过滤维度**，任意深度可叠加。
- UI 提供「披露深度滑块 / 展开层级」控件，从浅到深渐进披露。

| 功能 | 说明 | 优先级 |
|---|---|---|
| 分层全文搜索 | L1 标题/L2 主题/L3 正文/L4 思考 分档搜索 | P0 |
| 🆕 主题树浏览 | 按抽出的主题/子主题钻取 | P1 |
| 按项目/账号/agent/时间过滤 | 正交维度 | P0 |
| 结果高亮 + 上下文 | 命中高亮，前后文跳转 | P0 |
| 搜索语法 | `account:work` `layer:3` `type:thinking` | P1 |
| 正则/语义搜索 | 高级 / 本地 embedding | P2/P3 |
| 搜索历史 | 最近搜索保存 | P2 |
| 会话内搜索 | 单会话定位 | P1 |

### E. 会话浏览与阅读
| 功能 | 说明 | 优先级 |
|---|---|---|
| 会话列表 | 按时间/项目/账号/agent 排序 | P0 |
| 会话详情 | 完整对话，区分角色，标注账号 | P0 |
| 思考过程折叠 | 默认折叠展开 | P0 |
| 工具调用渲染 | 可折叠 | P1 |
| 文件变更展示 | 涉及文件列表 | P1 |
| 代码高亮 / Markdown | 语法高亮 + 渲染 | P0 |
| 导出 | Markdown/JSON/HTML | P1 |
| 复制上下文 | 单轮/整会话 | P2 |
| 收藏/标签/批注 | 标记、归类、笔记 | P1/P2 |
| 时间线视图 | 项目下横向时间轴 | P2 |

### F. 系统与配置
| 功能 | 说明 | 优先级 |
|---|---|---|
| 本地存储 | SQLite+FTS5 | P0 |
| 数据目录配置 / Agent 路径配置 / 排除规则 | 均可配置 | P1 |
| 🆕 账号映射配置 | account 显示名、工作/个人归类 | P1 |
| 敏感信息过滤 | 跳过/打码 | P2 |
| 自动更新索引 | 启动/定时增量 | P1 |
| 托盘 + 快捷键 | 后台常驻、全局呼出 | P2 |
| 数据统计 | 会话/项目/账号/agent 分布 | P2 |
| 多语言 | 中/英 | P2 |

---

## 四、完善度 / 成熟度模型
### 4.1 功能成熟度分级
| 等级 | 含义 | 判定 |
|---|---|---|
| 🥉 铜(C) | 可用但不完全自洽 | 主链路通，边界处理有缺口 |
| 🥈 银(S) | 主场景自洽 | 80% 主场景+边界+错误提示 |
| 🥇 金(G) | 打磨到位 | 性能达标、可配置、有自动化测试 |

### 4.2 全功能成熟度矩阵
| 功能模块 | MVP | 标准版 | 进阶版 |
|---|---|---|---|
| Agent 检测 | 铜(Trae/OpenCode/WorkBuddy) | 银(8+) | 金(插件化+手动增补) |
| 账号提取 | 铜 | 银 | 金(映射表+审计) |
| 主题/子主题抽取 | 银(规则抽取) | 金(L2 索引充分) | 金(聚类+LLM 可选) |
| 分层搜索 | 铜(L1+L3) | 银(L1-L4 全档) | 金(+语义) |
| 项目/账号聚合 | 铜 | 银 | 金(repo 感知) |
| 会话阅读 | 铜 | 银 | 金(批注/时间线) |
| 导出 | 铜(JSON) | 银(MD/HTML) | 金(模板/批量) |
| 系统配置 | 铜 | 银 | 金(托盘/统计) |

### 4.3 版本定义
- **V0.1 MVP**：Trae/OpenCode/WorkBuddy 会话 → 项目聚合 → L1标题+L3正文分层搜索 → 账号标注。闭环 S1/S2/S3。
- **V0.5 标准版**：L1-L4 全档 + 主题树 + 过滤全 + 导出 + 排除规则。
- **V1.0 正式版**：体验完善(托盘/快捷键/文件监听/统计/暗色)+性能达标，可公开。
- **V1.5+ 进阶**：语义搜索、插件化适配器、项目合并、跨设备加密同步、Web 版。

---

## 五、非目标与边界
❌ AI 对话客户端　❌ 云端同步　❌ 团队协作　❌ token 计费　❌ 实时监控告警　❌ 代码生成/修改　❌ 移动端

---

## 六、关键设计决策
1. **纯本地，无云端**。
2. **主动扫描为主，hook 可选增强**（对 Trae/OpenCode/WorkBuddy 优先看其既有 hook/插件/事件上报挂载点）。
3. **解析独立维护**；采集核心统一 **Node**。
4. **cwd 为项目主键**（非 git repo），normalize 大小写/尾斜杠/软链接。
5. **先 FTS5 全文，后语义**；分层搜索用「轻字段优先，深内容按需触发」。
6. 🆕 **账号与内容分层作为一等公民**贯穿采集→存储→检索。
7. 数据可持续：去重、增量、大文件保护。

---

## 七、数据模型 DDL（含账号 + 分层字段）

```sql
CREATE TABLE projects (
  id          TEXT PRIMARY KEY,       -- hash(normalize(cwd))
  path        TEXT NOT NULL,
  name        TEXT NOT NULL,
  custom_name TEXT,
  first_seen  INTEGER, last_seen INTEGER,
  meta        TEXT
);

CREATE TABLE accounts (              -- 🆕 账号维度
  id          TEXT PRIMARY KEY,       -- hash(raw_account + agent_id)
  agent_id    TEXT NOT NULL,
  raw_name    TEXT NOT NULL,
  display_name TEXT,
  kind        TEXT,                   -- work / personal / other
  UNIQUE(agent_id, raw_name)
);

CREATE TABLE sessions (
  id          TEXT PRIMARY KEY,       -- agent_id + original_session_id
  agent_id    TEXT NOT NULL,
  agent_session_id TEXT NOT NULL,
  project_id  TEXT REFERENCES projects(id),
  account_id  TEXT REFERENCES accounts(id),  -- 🆕
  cwd         TEXT,
  title       TEXT,                   -- L1
  subject     TEXT,                   -- 🆕 L2 主题节点
  parent_subject TEXT,                -- 🆕 子主题从属
  model       TEXT,
  created_at  INTEGER, updated_at INTEGER,
  turn_count  INTEGER,
  source_file TEXT,
  raw_format  TEXT,
  starred     INTEGER DEFAULT 0,
  tags        TEXT, notes TEXT,
  UNIQUE(agent_id, source_file)
);

CREATE TABLE turns (
  id          INTEGER PRIMARY KEY,
  session_id  TEXT REFERENCES sessions(id),
  seq         INTEGER,
  user_message    TEXT,               -- L3
  assistant_message TEXT,             -- L3
  thinking        TEXT,               -- L4
  tool_input      TEXT,               -- L4
  files_changed   TEXT,
  timestamp       INTEGER,
  UNIQUE(session_id, seq)
);

CREATE TABLE subjects (              -- 🆕 主题/子主题索引
  id        INTEGER PRIMARY KEY,
  session_id TEXT REFERENCES sessions(id),
  layer     INTEGER NOT NULL,          -- 2=主题 2.5=子主题
  subject   TEXT NOT NULL,
  parent_id INTEGER
);

CREATE VIRTUAL TABLE turns_fts USING fts5(
  user_message, assistant_message, thinking, tool_input,
  content='turns', content_rowid='id'
);
-- 另建标题/主题轻量索引层（L1/L2），避免浅层查询触全表
CREATE INDEX idx_sess_l1 ON sessions(title);
CREATE INDEX idx_sess_subject ON sessions(subject);
```

**检索策略（分层性能）**
- L1/L2：走 `idx_sess_l1` / `idx_sess_subject` 普通索引，秒返回、不触发 FTS。
- L3/L4：仅在绕过 L1/L2 或结果不足时走 `turns_fts`。
- 账号/项目/agent/时间为过滤条件，先过滤再检索，进一步缩小扫描面。

---

## 八、技术架构（定稿）
| 层 | 选型 | 说明 |
|---|---|---|
| 桌面框架 | **Tauri(Rust + Web)** | 轻量、跨平台、体积小 |
| 前端 | React + TypeScript | 生态好 |
| 存储/搜索 | SQLite + FTS5（+可选 sqlite-vec） | 全文 + 分层检索 |
| 采集/解析核心 | **Node.js（解析逻辑）** | 与 agent 生态一致，parse 直接复用 |
| 进程模型 | Tauri(Rust) 主进程 + Node 解析子进程 | 隔离采集，避免阻塞 UI |

**数据流**：启动/定时 → 扫描会话目录 → 增量比对(mtime+size) → 解析适配器(Node) → 提取(消息/思考/账号/主题) → 写 SQLite(分层索引) → 用户渐进披露式检索。

---

## 九、质量与风险
| 风险 | 应对 |
|---|---|
| agent 格式变更 | 适配器独立，失败降级原文索引 |
| 会话文件过大 | 大文件跳过/采样，流式解析 |
| 账号识别不准 | 账号映射配置 + 归一化，支持人工修正 |
| 敏感信息 | 本地存储 + 可选过滤 + 排除规则 |
| 跨平台路径 | Node path 统一，三端测试 |
| 索引膨胀 / 分层性能 | 去重 + 增量 + L1/L2 轻索引分流 |

**成功指标**
- MVP：索引 Trae/OpenCode/WorkBuddy；**L1/L2 搜索 <100ms，L3/L4 <500ms**（万级会话）；增量扫描无改动 <5s。
- 标准版：覆盖 8+ agent；分层首层 <100ms(十万级)；GitHub Star>1k。

---

## 十、里程碑（可独立交付）
- **M1 骨架+最小链路**：脚手架(Tauri+Node 解析子进程+SQLite 建表含账号/分层字段)→ Trae 适配器 → 索引 → L1标题+L3正文分层搜索 → 三视图。判据：改一个会话文件能搜到，且能在项目/账号维度标注。
- **M2 多 Agent + 增量 + 账号与主题抽取**：+OpenCode/WorkBuddy → 增量扫描 → 账号提取与映射 → 主题/子主题抽取(L2)。判据：三家全量索引，二次增量 <5s。
- **M3 渐进披露 + 阅读体验**：L1-L4 全档渐进式检索 + 主题树浏览 → 思考折叠/代码高亮 → 导出。
- **M4 系统化**：配置/排除/托盘/快捷键/文件监听/统计/账号映射配置。

---

## 十一、待确认项（本轮已确认，仅记录决议）
| 项 | 决议 |
|---|---|
| 命名 | ✓ AgentSessions |
| 技术栈 | ✓ Tauri + Node 解析核心 |
| 首批 agent | ✓ Trae / OpenCode / WorkBuddy 系列 |
| 功能取舍 | ✓ 不做取舍，全部保留深入优化 |
| 🆕 账号维度 | ✓ 新增（accounts 表 + 会话标注 + 过滤） |
| 🆕 内容分层/子主题渐进披露 | ✓ 新增（L1标题-L5账号，主题树，漏斗式检索） |

> 下一步建议：进入 M1，搭建 **Tauri + Node 解析子进程 + SQLite(含账号/分层字段)** 骨架，并先确认本机 Trae/OpenCode/WorkBuddy 各自的会话文件实际存储路径与格式，作为首个适配器开发的输入。